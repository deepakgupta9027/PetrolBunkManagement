spring:
  application:
    name: hr-service

  datasource:
    url: ${HR_DB_URL:jdbc:mysql://localhost:3306/hr_service_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    username: root
    password: Saraansh017@12
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        jdbc:
          time_zone: UTC

  sql:
    init:
      mode: always

server:
  port: 3003

gateway:
  trust:
    enabled: ${GATEWAY_TRUST_ENABLED:true}
    token: ${GATEWAY_INTERNAL_TOKEN:dev-gateway-internal-token-change-in-production}
